# Plan 08-02: File Intake And Guardrails

**Phase:** 8 Hidden Converter Foundation
**Status:** Completed

## Goal

Add the dependency foundation, local file intake, validation, and explicit
no-upload guardrails needed before conversion work begins.

## Tasks

- Add `mammoth` and `jszip` dependencies for later phases.
- Implement `.docx` file selection and drag/drop.
- Reject `.doc`, non-Word files, and oversized files.
- Display valid file status and validation errors.
- Include UI copy for no upload, no R2, no history, and no Worker API.

## Verification

- `pnpm typecheck` passes.
- `pnpm lint` passes.
- `pnpm build` passes.
