# Plan 09-02: Markdown Output Actions

**Phase:** 9 Markdown Conversion Core
**Status:** Completed

## Goal

Add the result panel, conversion status, copy action, and `.md` download action
for generated Markdown.

## Tasks

- Add a Markdown result panel with a read-only output area.
- Add visible idle, converting, converted, and error status states.
- Add copy-to-clipboard with visible success/failure feedback.
- Add `.md` download with a sanitized filename.
- Keep the hidden route private by leaving `/tools` and the homepage unchanged.

## Verification

- User can inspect generated Markdown after conversion.
- User can copy generated Markdown.
- User can download generated Markdown as a `.md` file.
- Status, warnings, and failure reasons are visible.
