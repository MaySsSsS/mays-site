# Plan 01-01: Cover Markup And Title Treatment

## Objective

Replace the current portal hero copy with semantic cover markup for the
`MAYS UNIVERSE` first viewport.

## Tasks

- Update `app/page.tsx` to introduce a cover section with decorative ink, burst, line, title, and cue layers.
- Keep the page static and server-rendered.
- Preserve portal CSS class compatibility for the not-found page.

## Must Haves

- `MAYS UNIVERSE` appears as the only visible cover text.
- No long copy, bio, or feature explanation appears in the cover.
- TypeScript remains strict-clean.
