# Plan 04-01: Tools Hub Worldbuilding

## Objective

Create the first `/tools` route with a holographic but readable hub experience.

## Tasks

- Add a new `app/tools/page.tsx` route for the tools hub.
- Design a clear hero and card layout that frames the branch as `Signal Lab`
  while still reading as a tools entrance.
- Show only the live `QR Studio` entry plus non-specific future placeholders.

## Must Haves

- `/tools` clearly reads as a tools hub.
- `QR Studio` is the only named live tool.
- Future expansion is implied without inventing undeveloped tool names.
