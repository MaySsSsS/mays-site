# Plan 05-02: QR Export And Reset

## Objective

Make `QR Studio` shippable by adding export and quick-reset flows.

## Tasks

- Support PNG export.
- Support SVG export.
- Add a reset flow that restores the base configuration.
- Make sure the `/tools` hub links into the finished tool cleanly.

## Must Haves

- PNG and SVG both download successfully.
- Reset works without a page refresh.
- No backend or storage dependency is introduced.
